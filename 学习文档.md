# 📚 ResearchAgent 学习文档

> 从原理到代码，彻底搞懂一个工业级 RAG 学术研究助手是怎么 built 的。
> 学完这份文档，你应该能：① 讲清 RAG 的完整流程和每个环节的优化点；② 读懂并修改项目中的每一行代码；③ 在面试中从容回答 RAG 相关问题；④ 基于这个项目做自己的扩展。

---

## 目录

1. [项目要解决什么问题](#1-项目要解决什么问题)
2. [整体架构设计](#2-整体架构设计)
3. [模块一：论文检索（PaperSearcher）](#3-模块一论文检索papersearcher)
4. [模块二：PDF 解析（PDFParser）](#4-模块二pdf-解析pdfparser)
5. [模块三：RAG 引擎（RAGEngine）—— 核心重点](#5-模块三rag-引擎ragengine-核心重点)
6. [模块四：文献综述生成（ReviewGenerator）](#6-模块四文献综述生成reviewgenerator)
7. [模块五：引用管理（CitationManager）](#7-模块五引用管理citationmanager)
8. [命令行入口与 Web 界面](#8-命令行入口与-web-界面)
9. [面试高频问题深度解析](#9-面试高频问题深度解析)
10. [扩展方向与进阶挑战](#10-扩展方向与进阶挑战)

---

## 1. 项目要解决什么问题

### 1.1 痛点分析

做学术研究的人都有这些痛点：

1. **论文检索效率低** —— 在 arXiv、Google Scholar 上搜论文，要一个个点开看摘要，筛选相关的
2. **PDF 阅读耗时** —— 一篇论文几十页，读完要花几小时，读完还容易忘
3. **文献综述难写** —— 要对比几十篇论文的方法、实验、贡献，整理成综述非常耗时
4. **知识管理混乱** —— 读过的论文散落在各个文件夹，想找的时候找不到

### 1.2 解决方案

ResearchAgent 用 RAG（检索增强生成）技术解决这些问题：

```
传统流程：搜论文 → 下载PDF → 人工阅读 → 整理笔记 → 写综述（几小时~几天）

ResearchAgent：输入主题 → 自动检索+下载 → 自动解析+索引 → 问答/生成综述（几分钟）
```

### 1.3 为什么这个项目有含金量

- **不是玩具级 Demo** —— 有完整的 PDF 解析、向量检索、综述生成，每个模块都有工程细节
- **技术栈主流** —— LangChain + ChromaDB + Streamlit，都是工业界常用的
- **有真实应用场景** —— 学术研究是真实需求，不是为了做项目而做项目
- **可扩展性强** —— 可以加很多高级功能（GraphRAG、多模态、重排序等）

---

## 2. 整体架构设计

### 2.1 架构图

```
用户输入（研究主题/问题）
       │
       ▼
┌──────────────┐     ┌──────────────┐
│ PaperSearcher│────▶│  本地 PDF    │
│  arXiv 检索   │     │  文件存储     │
└──────────────┘     └──────┬───────┘
                              │
                              ▼
                     ┌──────────────┐
                     │  PDFParser   │
                     │  论文解析     │
                     │ (标题/摘要/   │
                     │  章节/参考文献)│
                     └──────┬───────┘
                            │
                            ▼
                     ┌──────────────┐
                     │  RAGEngine   │
                     │  分块→向量化  │
                     │  →存储→检索   │
                     └──────┬───────┘
                            │
              ┌─────────────┼─────────────┐
              ▼             ▼             ▼
        ┌──────────┐ ┌──────────┐ ┌──────────────┐
        │ 智能问答  │ │ 综述生成  │ │ 引用管理      │
        │ (带引用)  │ │ (多维度)  │ │ (BibTeX导出) │
        └──────────┘ └──────────┘ └──────────────┘
```

### 2.2 设计原则

1. **模块化** —— 每个功能独立成模块，方便测试和替换
2. **可配置** —— 所有参数（chunk_size、top_k 等）都在 config.py 里，不用改代码
3. **降级容错** —— PDF 解析双引擎，一个失败自动用另一个
4. **可追溯** —— 所有回答都带引用来源，知道答案从哪来
5. **双端交互** —— CLI 适合批量处理，Web UI 适合演示和日常使用

### 2.3 数据流

```
离线阶段（只做一次）：
arXiv → 下载PDF → 解析文本 → 分块 → 向量化 → 存入ChromaDB

在线阶段（每次查询）：
用户问题 → 向量化 → 相似度搜索 → 取Top-K → 拼入Prompt → LLM生成 → 带引用的回答
```

---

## 3. 模块一：论文检索（PaperSearcher）

### 3.1 功能

- 通过 arXiv API 检索学术论文
- 支持关键词、分类、作者检索
- 下载论文 PDF 到本地

### 3.2 核心概念：arXiv API

arXiv 是全球最大的预印本平台，提供免费的 API。我们用 `arxiv` 这个 Python 库来调用。

**arXiv 高级检索语法：**
- `ti:agent` —— 标题中包含 agent
- `au:zhang` —— 作者是 zhang
- `cat:cs.AI` —— 分类是人工智能
- `abs:reinforcement` —— 摘要中包含
- 组合：`cat:cs.AI AND ti:agent`

### 3.3 代码讲解

**数据结构 Paper：**
```python
@dataclass
class Paper:
    title: str              # 论文标题
    authors: List[str]      # 作者列表
    abstract: str           # 摘要
    url: str                # 论文页面
    pdf_url: str            # PDF下载链接
    published: datetime     # 发布时间
    categories: List[str]   # 分类
```
用 dataclass 而不是 dict，是因为：① 有类型提示，IDE 能补全；② 有默认值，不容易出错；③ 可以加方法（to_dict、__str__）。

**检索方法 search：**
```python
def search(self, query: str, max_results: int = None) -> List[Paper]:
    search = arxiv.Search(
        query=query,
        max_results=max_results or self.max_results,
        sort_by=arxiv.SortCriterion.Relevance,
    )
    papers = []
    for result in self._client.results(search):
        paper = Paper(...)  # 转换为我们的数据结构
        papers.append(paper)
    return papers
```

**为什么要做一层封装？** 直接用 arxiv 库也行，但封装后：① 统一返回我们定义的 Paper 结构，下游模块不用关心 arxiv 的数据格式；② 可以加错误处理、缓存、重试；③ 以后换检索源（比如 Semantic Scholar）不用改下游代码。

**下载方法：**
```python
def download_paper(self, paper: Paper, save_dir: str = None) -> str:
    # 1. 生成安全的文件名（去掉特殊字符，限制长度）
    safe_title = "".join(c if c.isalnum() or c in " -_" else "_" for c in paper.title)
    filename = f"{safe_title[:80]}.pdf"

    # 2. 已下载就跳过（幂等性）
    if os.path.exists(filepath):
        return filepath

    # 3. 主方案：用 arxiv 库下载
    try:
        result.download_pdf(dirpath=save_dir, filename=filename)
    except Exception as e:
        # 4. 备用方案：直接用 requests 下载
        resp = requests.get(paper.pdf_url, timeout=30)
        with open(filepath, "wb") as f:
            f.write(resp.content)
```

**工程细节：**
- 文件名清理：论文标题可能有特殊字符（冒号、斜杠），直接用作文件名会报错
- 幂等性：已下载的文件不重复下载，节省时间和带宽
- 降级方案：主方案失败用备用方案，提高鲁棒性
- 礼貌延迟：下载后 sleep 1 秒，不给 arXiv 服务器太大压力

### 3.4 面试可能问

**Q：为什么不直接用 Google Scholar？**
A：Google Scholar 没有官方 API，爬取容易被封。arXiv 有官方免费 API，稳定可靠，而且计算机领域的论文基本都在 arXiv 上有预印本。

**Q：怎么处理检索结果太多的情况？**
A：① 限制 max_results；② 按相关度排序；③ 可以加筛选条件（时间范围、分类）；④ 后续可以用 LLM 做二次筛选。

---

## 4. 模块二：PDF 解析（PDFParser）

### 4.1 为什么 PDF 解析是难点

学术论文 PDF 不是普通文本文件，有很多复杂结构：
- **双栏排版** —— 左右两栏，简单提取会把两栏混在一起
- **公式** —— 大量数学公式，提取出来是乱码
- **表格** —— 二维表格结构，纯文本提取会丢失行列关系
- **图表标注** —— 图注、表注穿插在正文中
- **页眉页脚** —— 每页都有，重复出现
- **参考文献** —— 格式特殊，需要单独识别

### 4.2 双引擎策略

```
pypdf：速度快，纯 Python，适合快速提取全文
pdfplumber：能获取精确的布局信息（坐标、字体大小），适合提取表格
```

项目中默认用 pdfplumber（更精确），失败时自动降级到 pypdf。

### 4.3 核心功能讲解

**提取标题和作者（启发式方法）：**
```python
def _extract_title_and_authors(self, first_page_text):
    lines = [l.strip() for l in first_page_text.split("\n") if l.strip()]

    # 标题：前10行中，不含邮箱/链接，长度>15，不以句号结尾的行
    for i, line in enumerate(lines[:10]):
        if any(kw in line.lower() for kw in ["@", "arxiv", "abstract", "http"]):
            continue
        if len(line) > 15 and not line.endswith("."):
            title = line
            title_idx = i
            break

    # 作者：标题后面的几行，按逗号/and分割
    for line in lines[title_idx+1 : title_idx+5]:
        if "@" in line or "abstract" in line.lower():
            break
        parts = re.split(r",| and |&", clean)
        ...
```

**为什么用启发式而不是 ML 模型？**
- 启发式方法简单、快、不需要训练数据
- 学术论文格式相对规范，启发式能覆盖大部分情况
- 对于这个项目的定位（学习+实用），启发式足够了
- 生产环境可以用专门的学术 PDF 解析服务（如 GROBID、Cermine）

**章节识别：**
```python
SECTION_PATTERNS = [
    r"^(\d+(?:\.\d+)*)\s+([A-Z][A-Za-z\s\-:,]+)$",  # "1 Introduction"
    r"^([A-Z][A-Z\s\-]{2,})$",                         # "INTRODUCTION"
    r"^(Abstract|Introduction|Related Work|...)$",       # 常见章节名
]

def _is_section_title(self, line):
    # 依次匹配三种模式
    # 返回 (是否是标题, 标题文本, 级别)
```

**三种模式覆盖了绝大多数学术论文的章节标题格式：**
1. 数字编号（最常见）：`1 Introduction`、`2.1 Related Work`
2. 全大写：`INTRODUCTION`、`RELATED WORK`
3. 常见章节名（不带编号）：`Introduction`、`Conclusion`

### 4.4 面试可能问

**Q：PDF 解析有哪些更好的方案？**
A：① GROBID —— 专门的学术文献解析工具，用 CRF 模型，准确率很高；② Cermine —— 基于 SVM 的学术 PDF 解析；③ 视觉方案 —— 用目标检测模型识别标题、作者、表格区域；④ LLM 直接解析 —— 把 PDF 转图片给多模态模型。本项目用启发式是为了简单和可解释，生产环境推荐 GROBID。

**Q：怎么处理双栏排版？**
A：pdfplumber 能获取每个字符的坐标，可以根据 x 坐标判断是左栏还是右栏，先提取左栏再提取右栏。pypdf 不支持，所以复杂排版要用 pdfplumber 或更专业的工具。

---

## 5. 模块三：RAG 引擎（RAGEngine）—— 核心重点

这是整个项目最核心的模块，也是面试最常问的部分。

### 5.1 RAG 完整流程回顾

```
┌───────────── 离线索引阶段 ─────────────┐
│                                          │
│  原始文本 → 分块(Chunk) → 向量化(Embed) │
│       → 存入向量数据库(Vector DB)         │
│                                          │
└──────────────────────────────────────────┘

┌───────────── 在线检索阶段 ─────────────┐
│                                          │
│  用户问题 → 向量化 → 相似度搜索(Top-K)   │
│       → 可选重排序(Rerank) → 相关文档块   │
│                                          │
└──────────────────────────────────────────┘

┌───────────── 生成阶段 ─────────────────┐
│                                          │
│  相关文档块 + 用户问题 → 构造Prompt       │
│       → LLM生成 → 带引用的回答            │
│                                          │
└──────────────────────────────────────────┘
```

### 5.2 文本分块（Chunking）

**为什么要分块？**
- LLM 上下文窗口有限，不能把整本书都塞进去
- 检索时需要找到最相关的**片段**，而不是整篇文档
- 分块后每块有独立的向量，检索更精准

**项目中用的分块策略：递归字符分块（RecursiveCharacterTextSplitter）**

```python
self._text_splitter = RecursiveCharacterTextSplitter(
    chunk_size=500,        # 每块最多500字符
    chunk_overlap=50,      # 相邻块重叠50字符（10%）
    separators=["\n\n", "\n", ". ", "。", " ", ""],  # 优先按这些边界切
    length_function=len,
)
```

**递归字符分块的原理：**
1. 先尝试按第一个分隔符（`\n\n`，段落之间）切
2. 如果某段还是太长，按第二个分隔符（`\n`，行之间）切
3. 还太长就按第三个（`. `，英文句号）切
4. 依次降级，直到每块都小于 chunk_size

**比简单的固定字符分块好在哪？**
- 优先在语义边界处切分，不会把一个句子切成两半
- 保持语义完整性，检索和生成效果更好

**重叠窗口（Overlap）的作用：**
```
块1: [这是第一段。这是第二段的开头部分...]
块2: [...第二段的开头部分。这是第二段的结尾。]
         ↑ 重叠部分，保证上下文连贯
```
如果没有重叠，一个句子刚好被切在两块之间，检索到任何一块都不完整。重叠保证了上下文连贯。

**chunk_size 怎么选？**
| 场景 | 推荐 chunk_size | 原因 |
|------|----------------|------|
| 问答型（事实查找） | 200~300 | 小块检索更精准，减少噪音 |
| 长文理解（摘要/综述） | 500~1000 | 大块保留更多上下文 |
| 代码 | 按函数/类切 | 代码有天然的结构边界 |

### 5.3 向量化（Embedding）

**什么是 Embedding？**
把文本转换成一串数字（向量），意思相近的文本，向量距离就近。

```
"猫" → [0.1, 0.8, 0.3, ...]
"狗" → [0.15, 0.75, 0.28, ...]  ← 和"猫"距离近
"汽车" → [0.9, 0.1, 0.5, ...]   ← 和"猫"距离远
```

**项目中用 OpenAI 兼容的 Embedding API：**
```python
self._embeddings = OpenAIEmbeddings(
    model=settings.EMBEDDING_MODEL,     # text-embedding-3-small
    api_key=settings.EMBEDDING_API_KEY,
    base_url=settings.EMBEDDING_BASE_URL,
)
```

**常用 Embedding 模型对比：**
| 模型 | 维度 | 中文支持 | 速度 | 成本 |
|------|------|---------|------|------|
| OpenAI text-embedding-3-small | 1536 | 一般 | 快 | 低 |
| BGE-large-zh（智源） | 1024 | 好 | 中 | 免费（本地） |
| M3E-large | 1024 | 好 | 中 | 免费（本地） |
| GTE-large | 1024 | 好 | 中 | 免费（本地） |

**面试重点：本地模型 vs API 模型**
- API 模型：不用自己部署，效果稳定，但有调用成本和数据隐私问题
- 本地模型：免费、数据不出本机，但需要 GPU、效果可能稍差
- 项目中用 API 是为了简单，实际生产中中文场景推荐用 BGE 本地部署

### 5.4 向量数据库（ChromaDB）

**为什么不用普通数据库？**
普通数据库存的是精确匹配（`WHERE title = 'xxx'`），但我们需要的是**语义相似度搜索**——找"意思最相近"的文本，而不是"完全一样"的文本。向量数据库专门做这个。

**项目中的 ChromaDB 使用：**
```python
# 初始化持久化客户端（数据存在磁盘上，重启不丢失）
self._client = chromadb.PersistentClient(path=self.persist_dir)

# 创建/获取集合，用余弦相似度
self._collection = self._client.get_or_create_collection(
    name=collection_name,
    metadata={"hnsw:space": "cosine"},  # 余弦相似度
)

# 存入数据
self._collection.upsert(
    ids=chunk_ids,           # 唯一ID
    documents=chunks,         # 原文
    metadatas=chunk_metadatas, # 元数据（论文标题、章节等）
    embeddings=embeddings,    # 向量
)

# 检索
results = self._collection.query(
    query_embeddings=[query_embedding],
    n_results=top_k,
    where=filter_metadata,    # 元数据过滤
)
```

**余弦相似度（Cosine Similarity）：**
衡量两个向量方向的相似程度，值越大越相似（范围 -1~1）。
- 1：完全相同方向
- 0：垂直（不相关）
- -1：完全相反

ChromaDB 返回的是 distance（距离），所以 `score = 1 - distance` 就是相似度。

**元数据过滤（Metadata Filtering）：**
```python
# 只在特定论文中检索
results = self._collection.query(
    query_embeddings=[query_embedding],
    n_results=5,
    where={"paper_title": "ReAct: Synergizing Reasoning..."},
)
```
这在实际应用中很有用——比如用户说"根据我上传的这篇论文回答"，就可以只检索这篇论文的内容。

### 5.5 混合检索（Hybrid Search）

**纯向量检索的问题：**
- 对专有名词、缩写、数字的检索效果不好（比如 "GPT-4"、"BERT"）
- 向量是语义匹配，可能漏掉关键词精确匹配的结果

**BM25 关键词检索：**
传统的信息检索方法，基于词频和逆文档频率（TF-IDF 的改进版），擅长精确关键词匹配。

**混合检索 = 向量检索 + BM25，两者互补：**
```
向量检索：擅长语义匹配（"如何让AI使用工具" → "Tool Learning in LLMs"）
BM25：  擅长关键词匹配（"ReAct" → 标题含"ReAct"的论文）
合并后：两种都能找到，去重后按分数排序
```

项目中的简化实现：
```python
def search_with_bm25(self, query, top_k):
    # 1. 向量检索（取多一些）
    vector_chunks = self.search(query, top_k=top_k * 2)

    # 2. 简单关键词匹配（简化版BM25）
    query_terms = set(re.findall(r'\w+', query.lower()))
    for doc in all_documents:
        doc_terms = set(re.findall(r'\w+', doc.lower()))
        overlap = len(query_terms & doc_terms)
        if overlap > 0:
            score = overlap / len(query_terms) * 0.5  # BM25权重较低

    # 3. 合并去重，按分数排序
    merged = vector_chunks + bm25_chunks
    merged.sort(key=lambda x: x.score, reverse=True)
    return merged[:top_k]
```

> **注意**：项目中的 BM25 是简化实现。生产环境推荐用专门的混合检索方案，比如 ChromaDB 原生支持的混合检索，或者用 Elasticsearch/OpenSearch。

### 5.6 生成阶段（LLM 生成）

**Prompt 设计是关键：**
```python
prompt = f"""你是一个专业的学术研究助手。请基于以下检索到的文献内容回答用户的问题。

要求：
1. 只基于提供的文献内容回答，不要编造文献中没有的信息
2. 回答中引用文献时使用 [数字] 格式标注来源，如 [1]、[2]
3. 如果文献内容不足以回答问题，请明确说明"根据现有文献无法确定"
4. 回答要专业、准确、有条理
5. 最后列出所有引用的文献来源

检索到的文献内容：
{context}

用户问题：{query}

请给出你的回答："""
```

**Prompt 设计的几个要点：**
1. **角色设定** —— "专业的学术研究助手"，让 LLM 用专业语气回答
2. **明确约束** —— "只基于文献内容，不要编造"，减少幻觉
3. **引用格式** —— 明确要求用 `[数字]` 标注，方便溯源
4. **兜底机制** —— "不知道就说不知道"，避免强行回答
5. **上下文放在前面** —— 先给文献内容，再给问题，LLM 更容易聚焦

**为什么回答带引用很重要？**
- 可溯源 —— 用户可以查看原文验证答案
- 减少幻觉 —— LLM 知道要标注引用，会更谨慎
- 学术规范 —— 学术写作本来就要求引用

### 5.7 面试高频问题深度解析

**Q1：RAG 的完整流程是什么？每个环节的作用？**
> 参考 5.1 节的流程图，分三个阶段讲：离线索引（加载→分块→向量化→存储）、在线检索（问题向量化→相似度搜索→可选重排序）、生成（拼接上下文→LLM生成→带引用回答）。每个环节说清楚为什么需要它。

**Q2：文本分块有哪些策略？chunk_size 怎么选？**
> ① 固定字符分块（最简单，但可能切断句子）；② 递归字符分块（优先按段落/句子边界切，项目用的就是这个）；③ 语义分块（用 embedding 判断语义相似度，在语义变化处切）；④ 按结构分块（代码按函数、文档按标题）。chunk_size 看场景：问答型用 200~300，长文理解用 500~1000。还要加 overlap（10%~20%）保证上下文连贯。

**Q3：怎么提升 RAG 的检索准确率？**
> 这是经典问题，从多个维度回答：
> 1. **分块优化**：递归分块、语义分块、合适的 chunk_size 和 overlap
> 2. **检索优化**：混合检索（向量+BM25）、重排序（Rerank）、查询改写（Query Rewriting）
> 3. **Embedding 优化**：用更好的模型、领域微调的 embedding 模型
> 4. **索引优化**：分层索引（摘要+全文）、父文档检索（先检索小块，返回父文档的完整内容）
> 5. **元数据过滤**：按时间、类别、来源过滤，减少噪音

**Q4：RAG 和 Fine-tuning 的区别？什么时候用哪个？**
> RAG 是外部知识检索，知识存在向量数据库里，改文档就能更新，可溯源，成本低。Fine-tuning 是把知识训练进模型参数，需要大量标注数据，成本高，更新要重新训练。选择：知识频繁更新、需要溯源、私有知识库 → RAG；需要改变模型风格/语气、特定领域的输出格式、任务特定的推理能力 → Fine-tuning。实际中常结合（RAG 管知识，Fine-tuning 管风格）。

**Q5：怎么评估 RAG 系统的好坏？**
> 两个层面：① 检索质量 —— 命中率（检索到的块是否包含答案）、MRR（正确答案的排名）、NDCG；② 生成质量 —— 答案正确率、忠实度（是否基于检索内容，有没有幻觉）、相关性、完整性。可以用 RAGAS（专门的 RAG 评估框架）来自动化评估。也可以人工评估 + 构建测试集。

**Q6：向量数据库有哪些？怎么选？**
> Chroma（轻量本地，适合原型和小项目）、FAISS（Facebook出品，纯本地，超快）、Milvus（企业级分布式，生产环境）、Pinecone（全托管云服务，免运维）、Qdrant（Rust写的，高性能）、Weaviate（开源，支持混合搜索）。选择看规模和运维能力：小项目用 Chroma/FAISS，生产用 Milvus/Qdrant，不想运维用 Pinecone。

**Q7：RAG 有哪些进阶方向？**
> GraphRAG（知识图谱+RAG，适合复杂关系推理）、Multi-modal RAG（支持图片/表格/视频）、Agentic RAG（Agent自己决定什么时候检索、检索什么、用什么工具）、Self-RAG（模型自己判断需不需要检索、检索结果好不好）、HyDE（假设性文档嵌入，先生成假设答案再用它检索）、Parent-Document Retrieval（父子文档检索）。

---

## 6. 模块四：文献综述生成（ReviewGenerator）

### 6.1 功能

基于已索引的多篇论文，自动生成结构化的文献综述，包括：
- 摘要
- 多维度分析章节（研究方法、核心贡献、实验评估、应用场景）
- 研究方法对比表格
- 研究空白识别
- 未来研究方向
- 参考文献列表

### 6.2 设计思路

文献综述不是简单的"把论文摘要拼起来"，而是需要：
1. **理解每篇论文的核心贡献**
2. **对比不同论文的方法和结果**
3. **归纳共性和差异**
4. **识别未解决的问题**
5. **展望未来方向**

所以项目用了**分步生成**的策略，每个部分单独生成，最后组装。

### 6.3 核心流程

```python
def generate(self, topic, focus_areas):
    # 1. 检索相关内容
    rag_result = self.rag.query(topic, top_k=10)

    # 2. 生成摘要（200-300字）
    review.summary = self._generate_summary(topic, rag_result)

    # 3. 按关注方向生成各章节
    for area in focus_areas:
        section = self._generate_section(topic, area, rag_result)
        review.sections.append(section)

    # 4. 生成对比表格
    review.comparison_table = self._generate_comparison_table(topic, papers_info)

    # 5. 识别研究空白
    review.research_gaps = self._identify_gaps(topic, rag_result)

    # 6. 未来研究方向
    review.future_directions = self._generate_future_directions(topic, rag_result)

    # 7. 参考文献
    review.references = self.citations.generate_reference_list()
```

### 6.4 每个部分的 Prompt 设计

**摘要生成 Prompt：**
```
你是一个资深的学术研究者。请基于以下文献内容，为"{topic}"写一段200-300字的综述摘要。
要求：
1. 概括该领域的研究现状和主要进展
2. 提到关键的研究方法和代表性工作
3. 指出当前的主要挑战
4. 使用 [数字] 标注引用来源
5. 语言要学术化、精炼
```

**研究空白识别 Prompt：**
```
请基于以下文献内容，分析"{topic}"领域存在的研究空白和未解决的问题。
要求：
1. 列出3-5个主要的研究空白
2. 每个空白说明为什么重要、为什么还没解决
3. 使用 [数字] 标注引用
```

**为什么分步生成而不是一次生成整篇？**
- 每部分有专门的 Prompt，质量更高
- 可以控制每部分的长度和重点
- 某部分失败可以单独重试，不影响其他部分
- 更容易调试和优化

### 6.5 面试可能问

**Q：生成的综述质量怎么保证？**
A：① 检索质量是基础——检索到的论文要相关且全面；② Prompt 工程——每部分有专门的 Prompt，明确要求和格式；③ 分步生成——每部分单独优化；④ 人工审核——综述生成后需要人工检查和修改，LLM 生成的是初稿。

---

## 7. 模块五：引用管理（CitationManager）

### 7.1 功能

- 管理论文引用信息
- 自动生成引用键（如 `zhang2024agent`）
- 导出 BibTeX 文件
- 生成格式化的参考文献列表（APA/IEEE 格式）
- 从文本中提取 `[数字]` 引用标记

### 7.2 引用键自动生成

```python
def _generate_key(self, title, authors, year):
    # 第一作者姓
    first_author = authors[0] if authors else "unknown"
    parts = first_author.split()
    last_name = parts[-1] if len(parts) > 1 else first_author[:1]

    # 年份后两位
    year_short = year[-2:]

    # 标题第一个实词（跳过冠词介词）
    stop_words = {"a", "an", "the", "of", "for", "and", ...}
    for w in title_words:
        if w not in stop_words and len(w) > 2:
            first_word = w
            break

    return f"{last_name.lower()}{year_short}{first_word}"
    # 例如：zhang24attention
```

这是学术论文中常用的引用键格式（第一作者姓+年份+关键词），方便在 LaTeX 中引用。

### 7.3 BibTeX 导出

BibTeX 是 LaTeX 文档的参考文献标准格式：
```bibtex
@article{zhang24attention,
  title={Attention Is All You Need},
  author={Vaswani, Ashish and Shazeer, Noam},
  year={2017},
  journal={NeurIPS},
  url={https://arxiv.org/abs/1706.03762}
}
```

---

## 8. 命令行入口与 Web 界面

### 8.1 命令行入口（main.py）

用 argparse 实现子命令模式：
```
python main.py search "关键词"    # 检索
python main.py index              # 索引PDF
python main.py ask "问题"         # 问答
python main.py review "主题"      # 生成综述
python main.py stats              # 查看统计
```

**为什么用子命令模式？**
- 每个功能独立，参数清晰
- 类似 git 的使用方式，用户容易上手
- 方便扩展新功能

### 8.2 Web 界面（app.py）

用 Streamlit 实现，特点：
- 纯 Python 写前端，不用写 HTML/JS
- 自动刷新，开发快
- 适合数据应用和 AI Demo

**五个页面：**
1. 论文检索 —— 输入关键词，展示结果，可下载
2. PDF 解析与索引 —— 上传 PDF，自动解析索引
3. 智能问答 —— 聊天式问答，带引用
4. 文献综述 —— 输入主题，生成完整综述
5. 索引统计 —— 查看数据库状态

### 8.3 Streamlit 的核心概念

**session_state：** 跨页面/跨交互保持状态
```python
if "rag" not in st.session_state:
    st.session_state.rag = RAGEngine()  # 只初始化一次
```

**chat_message：** 聊天界面组件
```python
with st.chat_message("user"):
    st.markdown(question)
with st.chat_message("assistant"):
    st.markdown(answer)
```

---

## 9. 面试高频问题深度解析

### 9.1 项目相关

**Q：介绍一下你的这个项目。**
> 用 2.1 节的架构图思路，3 分钟讲清楚：① 解决什么问题（学术研究效率低）；② 整体架构（检索→解析→RAG→问答/综述）；③ 技术亮点（递归分块、混合检索、分步生成综述、引用溯源）；④ 效果（几分钟完成原来几小时的工作）。

**Q：你这个项目最大的技术挑战是什么？怎么解决的？**
> 推荐回答 PDF 解析或 RAG 检索准确率。比如："最大的挑战是学术论文 PDF 的解析。论文有双栏排版、公式、表格、页眉页脚，简单提取效果很差。我的解决方案是：① 用 pdfplumber 获取精确布局信息，比 pypdf 更准确；② 用启发式规则识别标题、作者、章节、参考文献；③ 双引擎降级，pdfplumber 失败自动用 pypdf。后续可以用 GROBID 等专业工具进一步提升。"

**Q：你做了哪些优化？**
> ① 分块优化：递归字符分块+重叠窗口，比固定分块效果好；② 检索优化：混合检索（向量+BM25），兼顾语义和关键词匹配；③ 工程优化：双 PDF 引擎降级、幂等下载、配置化参数；④ 生成优化：分步生成综述，每部分专门 Prompt。

### 9.2 技术深度

**Q：如果让你重新设计这个项目，你会怎么改进？**
> 这是考察你有没有深入思考。可以说：① 用 GROBID 替代启发式 PDF 解析，准确率更高；② 加 Rerank 重排序，用 Cross-Encoder 精排检索结果；③ 加 GraphRAG，构建论文之间的引用关系图谱，支持关系推理；④ 加用户反馈机制，用户可以点赞/点踩回答，用于优化检索和 Prompt；⑤ 支持多模态，解析论文中的图表。

**Q：这个项目怎么部署到生产环境？**
> ① 容器化：用 Docker 打包，docker-compose 编排（应用+向量数据库）；② API 化：用 FastAPI 把功能封装成 REST API，前端分离；③ 异步处理：PDF 解析和索引用 Celery 异步任务队列；④ 监控：加日志、指标、告警；⑤ 安全：API Key 管理、用户认证、数据隔离。

---

## 10. 扩展方向与进阶挑战

### 10.1 必做扩展（建议至少做 2 个）

1. **加 Rerank 重排序** —— 用 `sentence-transformers` 的 Cross-Encoder 模型对检索结果精排，这是提升 RAG 效果最有效的手段之一
2. **支持本地 Embedding 模型** —— 用 BGE-large-zh 替代 API，降低成本，保护隐私
3. **加查询改写（Query Rewriting）** —— 用 LLM 把用户的口语化问题改写成更适合检索的查询
4. **支持更多文献源** —— 接入 Semantic Scholar API、PubMed API，不只是 arXiv

### 10.2 进阶挑战

1. **GraphRAG** —— 提取论文中的实体和关系，构建知识图谱，基于图谱做检索和推理
2. **多模态 RAG** —— 解析论文中的图表（用多模态模型），支持"这张图说明了什么"这类问题
3. **Agentic RAG** —— 让 Agent 自己决定：什么时候检索、检索什么、要不要用工具、要不要多次检索
4. **个性化推荐** —— 基于用户的阅读历史，推荐相关论文
5. **协作功能** —— 多人共享文献库、标注、笔记

### 10.3 写进简历的亮点

做完扩展后，简历可以这样写：
```
智能学术研究助手 | Python, LangChain, ChromaDB, Streamlit
- 设计并实现基于 RAG 的学术论文研究助手，支持 arXiv 论文检索、PDF 解析、语义检索和文献综述自动生成
- 优化检索 pipeline：递归字符分块 + 混合检索（向量+BM25）+ Cross-Encoder 重排序，检索命中率提升 40%
- 实现多论文对比分析和自动引用管理，生成带完整引用的综述报告，支持 BibTeX 导出
- 开源项目，GitHub XX Star，Streamlit Cloud 在线 demo
```

---

## 总结

学完这个项目，你应该掌握了：
1. ✅ RAG 的完整流程和每个环节的优化方法
2. ✅ 学术 PDF 解析的技术难点和解决方案
3. ✅ 向量数据库的原理和使用
4. ✅ Prompt 工程的实战技巧
5. ✅ 模块化的项目架构设计
6. ✅ CLI + Web 双端交互的实现
7. ✅ 面试中 RAG 相关问题的回答思路

**下一步：把项目跑起来，做至少 2 个扩展，然后准备面试！**

---

*文档版本：v1.0 | 最后更新：2026-09*
